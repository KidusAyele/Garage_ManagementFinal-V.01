@model IEnumerable<Garage_ManagementFinal.Entry_Form>

@{
    ViewBag.Title = "unrepairedCar";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}
@{

    if (TempData["SuccessMessage"] != null)
    {
        <p class="alert alert-success">
            @TempData["SuccessMessage"]
        </p>
    }
    if (TempData["SuccessMessage1"] != null)
    {
        <p class="alert alert-danger">
            @TempData["SuccessMessage"]
        </p>
    }
}

<h2>Unrepaired Car</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
          Plate No
        </th>
        <th>
           Brand
        </th>
        <th>
           Owner Name
        </th>
        <th>
           Entry Date
        </th>
     
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.region)
                @Html.DisplayFor(modelItem => item.code)
                @Html.DisplayFor(modelItem => item.Alpha)
                @Html.DisplayFor(modelItem => item.plate_no)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owner_name)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
         

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info" })
                @Html.ActionLink("Check Out", "leave", new { id = item.Id }, new { @class = "btn btn-success" })
            </td>
        </tr>
    }

</table>
